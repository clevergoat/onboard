<div class="six columns offset-by-three">

	<%= simple_form_for [@job, @submission] do |f| %>

		<%= f.input :cover_letter, input_html: { rows: 12 } %>
		<%= f.input :cv, label: "Upload CV" %>

		<%= f.button :submit, "Apply" %>

	<% end %>

</div>